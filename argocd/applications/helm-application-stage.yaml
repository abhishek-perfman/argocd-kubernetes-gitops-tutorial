apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grade-submission-helm-stage
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/abhishek-perfman/argocd-kubernetes-gitops-tutorial.git
    targetRevision: HEAD
    path: helm/grade-submission
    helm:
      valueFiles:
        - values-stage.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: grade-helm-stage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
